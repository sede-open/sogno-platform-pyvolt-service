kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: jack
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 5000
        hostPort: 5000
        protocol: TCP
      - containerPort: 5001
        hostPort: 5001
        protocol: TCP
