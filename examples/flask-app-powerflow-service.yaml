apiVersion: v1
kind: Service
metadata:
  name: flask-app-powerflow-service
spec:
  selector:
    app: flask-app-powerflow
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080  # You can choose any port in the range 30000-32767
  type: NodePort
